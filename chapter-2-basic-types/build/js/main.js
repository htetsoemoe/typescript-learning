"use strict";
let myName;
let meaningOfLife;
let isLoading;
let album; // 'any' can be 'string', 'number', 'boolean', 'array', 'object'
myName = 'John Doe';
meaningOfLife = 32;
isLoading = true;
album = {
    "name": "Metallica",
    "album": "black",
    "year": 1999
};
const sum = (a, b) => {
    return a + b;
};
// Union Type
let postId;
let address;
let isActive;
// RegExp Type
let re = /\w+/g;
